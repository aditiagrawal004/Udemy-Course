//Introduction to Union
->Derived datatype
->Syntax is same as Structure.
->In a structure each memory has its own memory locations,
  where as union share the same memory.

  union union_name
  {
      datatype member1;
      datatype member2;
      datatype member3;
  }u1;
  union union_name u1;
  u1.member1;
